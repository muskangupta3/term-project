<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/@picocss/pico@1/css/pico.classless.min.css"
    />
    <link href="/css/style.css" rel="stylesheet" />
    <title>Sub posts</title>
  </head>
  <body>
    <main>
      <div class="authOptions">
        <h1>Posts in <%= subName %></h1>
        <a role="button" href="/posts">Home</a>
      </div>

      <% if (posts.length > 0) { %> <% posts.forEach(post => { %>
      <div
        class="individualPost"
        style="border: 1px solid #ddd; padding: 1rem; margin-bottom: 1rem"
      >
        <div
          style="
            display: flex;
            justify-content: space-between;
            align-items: center;
          "
        >
          <a href="/posts/show/<%= post.id %>"
            ><strong><%= post.title %></strong></a
          >
        </div>
        <p><%= post.description %></p>

        <% if (user) { %>
        <div style="display: flex; align-items: center; gap: 0.5rem">
          <div style="margin-top: 0.5rem">
            <small
              >👍 <%= post.upvotes || 0 %> &nbsp;&nbsp; 👎 <%= post.downvotes ||
              0 %></small
            >
          </div>
        </div>
        <% } %>
      </div>
      <% }) %> <% } else { %>
      <p>No posts found in this sub.</p>
      <% } %>
    </main>

    <style>
      .active-vote {
        background-color: #007f5f;
        color: white;
        font-weight: bold;
      }
    </style>
  </body>
</html>
